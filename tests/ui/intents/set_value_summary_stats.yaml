# Scenario: Verify summary stats row after analysis
#
# Related:
#   pages: [set-value]

description: >
  After running an analysis, the summary row shows Total Cards (nonzero),
  With Prices (nonzero, and fewer than Total Cards — the fixture leaves the
  foil-only printings without a nonfoil price), Median as a dollar amount,
  and four tier percentages (Chaff, Modest, High, Premium) that sum to
  roughly 100%.
