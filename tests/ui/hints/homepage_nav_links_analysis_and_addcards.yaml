start_page: /
involves:
  - "nav-group 'Analysis' with links: Crack-a-Pack (/crack), Explore Sheets (/sheets), Browse Sets (/sets), Set Value (/set-value)"
  - "nav-group 'Add Cards' with links: Upload (/upload), Recent (/recent), Corners (/ingest-corners), Manual ID (/ingestor-ids), Orders (/ingestor-order), CSV Import (/import-csv)"
  - "OCR subgroup .nav-subgroup with .sub-label 'OCR' containing Upload and Recent"
  - "/sets renders no heading of its own — it lands as a.set-tile links under #sets-body"
notes: >
  1. Navigate to /. 2. Assert OCR sub-label is visible in Add Cards group.
  3. Assert the Analysis group holds exactly 4 links, so a link added to the
  group later cannot go unexercised the way Browse Sets did.
  4. Click "Crack-a-Pack" link, wait for page, navigate back.
  5. Click "Explore Sheets", wait for page, navigate back.
  6. Click "Browse Sets", wait for the set tiles, navigate back.
  7. Click "Set Value", wait for page, navigate back.
  8. Click "Upload", wait for page, navigate back.
  9. Click "Recent", wait for page, navigate back.
  10. Click "Corners", wait for page, navigate back.
  11. Click "Manual ID", wait for page, navigate back.
  12. Click "Orders", wait for page, navigate back.
  13. Click "CSV Import", wait for page, navigate back. 14. Screenshot.
