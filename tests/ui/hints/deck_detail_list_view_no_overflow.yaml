start_page: /decks/1
involves:
  - ".builder-layout two-column grid with card preview and deck list"
  - "#type-groups multi-column layout with type-grouped card rows"
  - "body should have no horizontal overflow (scrollWidth <= clientWidth)"
fixture_data:
  deck_id: 1
  deck_name: "Bolt Tribal"
notes: >
  Set localStorage deckDetailView to 'list' before navigating to ensure
  list view loads. Navigate to /decks/1. Wait for the type groups to
  render. Verify #type-groups is visible. Use page.evaluate to check
  that document.body.scrollWidth <= document.body.clientWidth (no
  horizontal overflow). Take a screenshot of the final state.
